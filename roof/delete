 '''
    #get commandline arguments
    preloaded_weights = True
    try:
        opts, args = getopt.getopt(sys.argv[1:],"n:")
    except getopt.GetoptError as e:
        print (str(e))
        sys.exit(2)

    for o, a in opts:
        if o == '-n':
            preloaded_weights = False
    '''
